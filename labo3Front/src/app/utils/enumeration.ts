export enum Sector {
    GROCERY, FASHION, CAR, FURNITURE, HOME_APPLIANCE
}

export enum AccessLevel {
    CUSTOMER, MANAGER, ADMINISTRATOR
}

export enum JuridicalStatus {
    PME, SA, SARL, SPRL, ONG, ASBL, EURL
}

export enum PayementMethod {
    DEBIT_CARD, CREDIT_CARD, CASH, PAYPAL
}
